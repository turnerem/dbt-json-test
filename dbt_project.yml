name: 'jsontest'
version: '1.0.0'
profile: 'default'

models:
  jsontest:
    +meta:
      orchestra_anomaly_detection: true
    staging:
      +materialized: view
    marts:
      +materialized: table
